export const GAME_SCORE_DATA_KEY = "magic-cups-data";
export const PLAYER_ID_DATA_KEY = "playerId";

export const HEIGHT = 768;
export const HALF_HEIGHT = HEIGHT / 2;
export const QUARTER_HEIGHT = HEIGHT / 4;
export const QUARTER_HEIGHT_OUTOFCLICKS = QUARTER_HEIGHT + 180;

export const WIDTH = 1152;
export const HALF_WIDTH = WIDTH / 2;
export const QUARTER_WIDTH = WIDTH / 4;

export const gameAreaWidth = WIDTH * 0.92;
export const gameAreaHeight = HEIGHT * 0.65;
export const gameAreaX = WIDTH / 2;
export const gameAreaY = HEIGHT / 2 + 10;

export const cupY = Math.round(HEIGHT * 0.70);
export const cloudY = Math.round(HEIGHT * 0.40);
export const gemY = Math.round(HEIGHT * 0.40);

export const START = 0;
export const END = 119; 

// Background Colour
export const BEIGE = "#FFF1CE";
export const WHITE = "#ffffff";
